{
  "name": "dkm-cakes-astro",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && powershell -Command \"Copy-Item -Path 'dist\\*' -Destination '.' -Recurse -Force -ErrorAction SilentlyContinue\""
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.9",
    "sharp": "^0.35.3"
  }
}
