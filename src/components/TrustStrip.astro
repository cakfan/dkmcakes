---
import config from "../data/config.json";
const items = [...config.trustStrip, ...config.trustStrip];
---

<div class="strip">
  <div class="strip-track">
    {items.map((item) => <span>{item}</span>)}
  </div>
</div>
